def rec(i):
  if i == 1: return 1
  return rec(i-1) + rec(i // 2) 

print(rec(100))


"""
def iter(n): 
    if(n == 1):
        return 1
    else:
        x = 1
        y = 1
        for i in range(1,n):
            z = (x + y // 2)
            x = y
            y = z
            return y
"""
